spring:
  application:
    name: ITest
  jpa:
    open-in-view: true
server:
  port: 8085

feign:
  client:
    config:
      default:
        url: "http://localhost:8080/engine-rest/"
        loggerLevel: BASIC

logging:
  level:
    org.camunda.community.rest.client.api: DEBUG
